name: Sync Publications from Zotero

on:
  workflow_dispatch:
  schedule:
    - cron: "25 7 * * 0"  # Sundays 07:25 UTC (~03:25 ET)

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      ZOTERO_GROUP_ID: 6271906   # Ron Weiss Publications group
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install requests

      - name: Fetch Zotero library (CSL-JSON)
        run: python scripts/fetch_from_zotero.py

      - name: Commit changes (if any)
        run: |
          if [[ -n "$(git status --porcelain)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add data/publications.zotero.json
            git commit -m "chore(pubs): update from Zotero [skip ci]"
            git push
          else
            echo "No changes."
          fi
